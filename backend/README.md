# OptiEDT backend

Python package `optiedt`: the HTTP API (`optiedt.api`), the solver worker (`optiedt.worker`),
the optimization engine (`optiedt.solver`) and the independent evaluator
(`optiedt.evaluation`). See `docs/development/setup.md` at the repository root.
